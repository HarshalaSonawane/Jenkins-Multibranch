pipeline {
    agent any
    stages {
        stage('Production Deploy') {
            steps {
                echo "Deploying stable build to Production live servers..."
            }
        }
    }
}
