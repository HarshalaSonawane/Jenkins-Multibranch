pipeline {
    agent any
    stages {
        stage('Dev Testing') {
            steps {
                echo "Running tests on Dev environment..."
                sh "echo 'Testing complete'"
            }
        }
    }
}
